export default function Layout ({ children }){
    return(
        <section className="bg-zinc-900 w-full p-10">
            <p className="mb-4"> Admin Area / Subscribe</p>
            {children}
        </section>
    )
}